{{~#each results~}}
    {expand:title={{testCase.name}}: {{result}} }
    {{~#each byDriver}}

h3. {{@key}} {{#if error}}{status:color=red|title=FAIL}{{else}}{status:color=green|title=SUCCESS}{{/if}} {{duration elapsedTime}}
        {{~#each commandResults}}

# {{#if error}}{status:color=red|title=FAIL}{{else}}{status:color=green|title=SUCCESS}{{/if}} {{duration elapsedTime}}\\
{{command.type}}("{{command.target}}", "{{command.value}}")\\
    {{~error~}}
            {{#if resultObject}}
                \\ !{{resultObject}}!
            {{/if}}
        {{/each}}
    {{/each}}
    {expand}
{{~/each}}